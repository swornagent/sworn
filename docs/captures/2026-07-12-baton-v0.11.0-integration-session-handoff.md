---
title: "Session handoff — Baton v0.11.0 integration: dogfood → ADR-0013 → feedback loop → re-vendor; next = Phase-4 specs"
date: 2026-07-12
author: Brad (Coach) + Claude (orchestrator)
branch: release/v0.1.0
---

# Session handoff — 2026-07-12 (Baton v0.11.0 integration)

## 0. TL;DR

A resume-from-unclean-exit dogfood surfaced 7 findings → became sworn **ADR-0013
(capability-based model selection, ratified)** → the protocol half went upstream
and came back as **Baton v0.11.0**, which was **re-vendored into the binary**
(feedback loop physically closed, one session). The **contract-edge-gates**
release was then replanned to become the **Baton v0.11.0 integration release**
(5 tracks / 12 slices); its decomposition is captured but the **9 new slice specs
are unwritten — that is the next session's job**.

## 1. Shipped & committed on `release/v0.1.0`

| Commit | What |
|---|---|
| `957de6d` | ADR-0013 ratified (Accepted) + ADR-0011 backfilled + dogfood-findings capture + Baton handoff |
| `0a56800` | **Baton protocol v0.10.0 → v0.11.0 re-vendor** — full suite green (47 pkg) |
| `841158e` | qa-runbook addendum to the v0.11.0 integration handoff (Fable session) |

**On `release-wt/2026-07-11-contract-edge-gates`:** `780aec0` — replan addendum
(the finalized 5-track/12-slice decomposition).

v0.11.0 is **live locally**: `make install` done (binary reports "baton-protocol
on Baton v0.11.0"); `sworn doctor --sync-baton` done (~/.claude/baton refreshed,
rule 12 present).

## 2. Key decisions (durable)

- **ADR-0013 (Accepted, Coach-ratified)** `docs/adr/0013-capability-based-model-selection.md`:
  constrain on CAPABILITIES not models (hard eligibility gate); eval is a SOFT
  prior, never a gate (explore/exploit avoids rich-get-richer freeze); two-schema
  split — `capability-policy-v1` → Baton (taxonomy + per-role required caps),
  routing-preferences → sworn (seeded by config OptimizeMode/PassRateFloor);
  provider-registry foundation (proactive metadata+pricing, ~/.config/sworn cache,
  reactive fallback). Findings 2/3 are its first consumers; the router is the third.
- **ADR-0011 backfilled** `docs/adr/0011-structured-outputs-keystone.md`: was
  implemented ~2026-06-30 and cited in ~25 sites but never documented;
  reconstructed with a section map so the in-code `§…` citations resolve.
- **sworn→Baton feedback loop FORMALIZED**: protocol findings go upstream, not
  buried as engine bugs. Handoff `docs/captures/2026-07-12-baton-handoff-capability-policy-and-protocol-updates.md`
  → Baton v0.11.0 (baton#62/#63 + independent Rule 12 baton#64/#65).
- **Baton residue cleaned**: 868M `~/sworn-eval-*` clones removed; opencode+codex
  baton installs removed (Brad reinstalls once sworn settles). `~/.sworn/.env`
  verified keys-only, in sync (SWORN_DIRECT=1 = direct-to-provider, own keys).

## 3. The dogfood findings (full record in the capture)

`docs/captures/2026-07-12-resume-recovery-dogfood-findings.md` — 7 findings,
sworn-vs-Baton triaged, with the capability-based-selection design + eval-as-prior
+ two-schema split. Findings closed by v0.11.0 vendoring the rule text:
1 (resume-reset, Rule 11), 5 (mock-as-code, Rule 10), 6 (autonomous gate, Rule 9),
plus Rule 12 guard-fidelity (new). Findings 0 (--dry-run ignored under --parallel)
and 4 (max-turns too low) are pure engine; 2 (structured-output — providers omit
the `Structured` field, NOT a grok limit) and 3 (cost re-derived from a hardcoded
table — read provider-reported cost) motivate the registry.

## 4. IMMEDIATE NEXT STEP — Phase-4 specs for the v0.11.0 integration release

The replan analysis is DONE and captured in the release's intake addendum
(`docs/release/2026-07-11-contract-edge-gates/intake.md`, committed `780aec0` on
`release-wt/2026-07-11-contract-edge-gates`). The next session writes the specs.

**Continuation handshake (Rule 6):** the release worktree exists at
`~/projects/sworn-worktrees/release-2026-07-11-contract-edge-gates` on branch
`release-wt/2026-07-11-contract-edge-gates`, based on `release/v0.1.0` @ `841158e`
(= PLANNER_START_SHA). It is clean. Reconciled state at replan: pure-plan, all
slices `planned`, no in-flight work, no drift, no BLOCKED. Prior dogfood
worktrees/branches were removed.

**The decomposition (from the intake addendum) — 5 tracks, 12 slices, all parallel (disjoint touchpoints):**
- T1-lint-contracts (exists): S01-registry, S02-mock-parity
- T2-assemble (exists): S03-assemble (+ qa-runbook render)
- T3-capability-selection (new): S04-provider-registry → S05-capability-eligibility → S06-routing-preferences
- T4-loop-fidelity (new): S07-resume-worktree-reset, S08-dry-run-parallel-honored, S09-loop-max-turns, S10-autonomous-design-authority
- T5-verify-fidelity (new): S11-guard-fidelity-gate (Rule 12), S12-mock-code-construct (Rule 10)

Touchpoint calls already made: Rule-10 fix = separate slice on `internal/gate/mock.go`
(disjoint from S02's `internal/lint/`); loop core `run/{slice,parallel}.go` chokepoint
keeps resume/dry-run/max-turns/rule9 in T4 (internally sequential); routing/eligibility
go in `run/resolve.go` (not slice.go) and rule9 reads a PROJECT config → 5 tracks disjoint.

**Phase-4 to-do:** write `spec.json` + `status.json` for S04–S12 (bounded EARS ACs
— note v0.11.0's new Rule-8 bounded-AC requirement), set each slice's `track`,
update `board.json` to the 5 tracks (current has only T1/T2 — add T3/T4/T5 and move
nothing existing), re-render `index.md`, validate against `board-v1`, commit to
`release-wt`, then Step-6 propagate (all tracks planned → each first `/implement-slice`
branches from `release-wt`). Per-slice scope is spelled out in the intake addendum.
Do this in a FRESH session (better spec fidelity than a marathon tail).

## 5. Other open threads

- **Config-layer replan → loop-cli-ux** (task #4, independent): JSON `config.json`
  settings + provider keys into `credentials.json` (drop SWORN_ prefix; env vars as
  overrides), SWORN_DIRECT → `config.json` "routing", TUI coverage, retire
  `~/.sworn/.env` behind a shim. Design in the dogfood capture (row R) + memory.
  NOTE: capability-based principle means config expresses capability POLICY, not
  model pins — keep loop-cli-ux config capability-aware.
- **W4 qa-runbook** (addendum `841158e`): rule text + template are post-v0.11.0 on
  baton main (rules-content 0.7.1) — need a **v0.11.1 vendor bump** OR implement W4's
  runbook from the addendum spec (advisory prose). Decide when W4 is scheduled.
- **W1 / sworn#88**: already delivered by S14-blocked-terminal (verified per the
  v0.11.0 handoff §3) — confirm coverage and CLOSE sworn#88 (not a slice).
- **Baton is ahead of v0.11.0**: local baton HEAD `e8c10d7` has baton#66/#67
  (qa-runbook) past the tag. A future v0.11.1 pulls those.
- **Pre-existing doctor noise**: `sworn doctor` exits 1 on render-drift +
  status-timestamp errors for OLD June releases (safe-parallelism,
  conformance-foundation) — unrelated to the vendor; a separate cleanup if wanted.
- **Deploy/ship**: slices stay `verified` until release/v0.1.0 deploys to prod,
  then flip to `shipped` (`/mark-shipped`). release/v0.1.0 was pushed earlier.

## 6. Pointers / artifact map

- ADRs: `docs/adr/0013-...`, `docs/adr/0011-...`
- Dogfood + design: `docs/captures/2026-07-12-resume-recovery-dogfood-findings.md`
- Upstream handoff: `docs/captures/2026-07-12-baton-handoff-capability-policy-and-protocol-updates.md`
- v0.11.0 integration handoff + addendum: `docs/captures/2026-07-12-baton-v0.11.0-integration-handoff.md`
- Replan decomposition: intake addendum @ `780aec0` on `release-wt/2026-07-11-contract-edge-gates`
- Vendored contract: `internal/adopt/baton/VERSION` (v0.11.0 @ 9f50909), `internal/baton/schemas/capability-policy-v1.json`
- Project memory: `project_capability_based_selection_2026_07_12.md` (index-linked)
