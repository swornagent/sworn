# Captain Trial Log — release 2026-06-27-conformance-foundation

| Date | Slice | Pins (mech/mem/esc) | Decision | One-line lesson |
|---|---|---|---|---|
| 2026-06-28 | S17-journeys-declare | 4 mech / 1 mem / 0 esc | PROCEED | `omitempty` on a spec-required field is silent data-loss; test must assert the field value, not just CheckPass; and AC7 for a gate-wiring slice needs transitive tracing back to the gate implementer's slice (S05). |
| 2026-06-28 | S05-merge-gate-oracle | 7 mech / 0 mem / 0 esc | PROCEED | A spec deferral ("X doesn't exist yet, stub it") goes stale the moment its dependency lands — recheck live state: S17 shipped journeys.json + ratified, so the merge-gate's journey check is real, not a stub. Also: a working-tree-mutating gate is Rule 11 territory — clean-tree assert is necessary but not sufficient; assert the *target* worktree/branch too. |
| 2026-06-28 | S06-invariant2-enforcement | 4 mech / 0 mem / 0 esc | PROCEED | "Mock oracle" in a spec AC implies an injection seam the design must actually provide — trace it to a concrete ParallelOptions field, because board.SliceState/OracleReader don't carry planned_files and the unit tests run outside a real repo. Also: "extend the frontmatter parser" is wrong when the data lives in the markdown body, not the frontmatter — read the file structure before trusting a reuse claim. |
| 2026-06-28 | S07-pause-resume-committed | 3 mech / 0 mem / 3 esc | NEEDS_COACH | Read the live function before trusting the spec's diagnosis: `findFirstNonTerminal` reads *no* state (returns slices[0]) and routing is already committed via the oracle — so the spec's "reads working-tree" premise is false. And in a forward-only frontier walk, marking `implemented` terminal (spec AC2) makes resume *skip* unverified slices — the opposite of the goal. |
