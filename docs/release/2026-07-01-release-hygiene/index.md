---
title: 'Release board — 2026-07-01-release-hygiene'
description: 'Rendered view of board.json + slice records. Generated by `sworn render 2026-07-01-release-hygiene` — do not hand-edit; edit board.json and re-render.'
---

# Release board: 2026-07-01-release-hygiene

> Generated by `sworn render`. This file is a deterministic view of `board.json`
> plus each slice's `spec.json`/`status.json` — never hand-authored.

## Tracks

| Track | Slices | depends_on | State |
|-------|--------|------------|-------|
| `T1-version-hygiene` | `S01-embedded-version`, `S02-version-bump-ci-gate` | — | planned |

## Slices

| Slice | Track | Outcome | State | E×C |
|-------|-------|---------|-------|-----|
| `S01-embedded-version` | `T1-version-hygiene` | Running `sworn version` (or `sworn --version`) reports the real version from a single embedded source for ANY build method — `go build`, `go install`, or `make` — instead of the 0.0.0-dev placeholder, while release builds can still override via ldflags. | planned | chore |
| `S02-version-bump-ci-gate` | `T1-version-hygiene` | A pull request that targets `main` without bumping the version is blocked by a fail-closed CI gate, so a release can never ship without the reported version advancing. | planned | chore |

## Touchpoint matrix

Every planned-write file × track. A file marked under two tracks is a
collision — disjointness is what licenses tracks to run independently.

| File | T1-version-hygiene |
|------|----|
| `.github/workflows/version-bump.yml` | ✓ |
| `Makefile` | ✓ |
| `cmd/sworn/main.go` | ✓ |
| `internal/version/version.go` | ✓ |
| `internal/version/version.txt` | ✓ |
| `internal/version/version_test.go` | ✓ |
| `scripts/check-version-bump.sh` | ✓ |

## Dependency graph

```
T1-version-hygiene  (root)
```
