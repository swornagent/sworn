{
  "$schema": "https://baton.sawy3r.net/schemas/spec-v1.json",
  "slice_id": "S01-versioned-local-records",
  "release": "2026-07-15-local-first-account-safety",
  "track": "T1-local-account-boundary",
  "user_outcome": "A SwornAgent user upgrades from the shared credential envelope without losing provider keys; an unused issuer-less legacy session is safely removed, and only a fresh issuer-bound login can create account state.",
  "rationale": "The current field-preserving envelope contains unrelated secrets under one unversioned corruption and logout boundary. This slice owns C-01 and the storage boundary for C-02, preserves the provider file path for compatibility, and makes the split converge through a verified provider rewrite without guessing authority for an issuer-less bearer token.",
  "touchpoints": [
    "internal/credentials/store.go",
    "internal/credentials/store_test.go",
    "internal/model/credentials.go",
    "internal/model/credentials_test.go",
    "internal/account/account.go",
    "internal/account/account_test.go",
    "cmd/sworn/credentials_integration_test.go",
    "cmd/sworn/doctor.go",
    "cmd/sworn/doctor_test.go",
    "cmd/sworn/init.go",
    "cmd/sworn/init_test.go"
  ],
  "in_scope": [
    "Define and validate provider-credentials-v1 at the existing credentials.json path and account-session-v1 at account.json, each written 0600 inside a 0700 configuration directory.",
    "Replace field-name mutation as the long-term authority with record-specific stores that reject malformed or unsupported versions without overwriting source bytes.",
    "Implement one idempotent migration routine that validates the legacy composite, requires recognized non-account fields to be consumed by their owning migrations and rejects unknown fields, constructs a versioned provider record preserving the complete providers map, writes it to a 0600 temporary file, syncs it, re-reads and deep-compares the map, then atomically replaces credentials.json.",
    "Discard issuer-less legacy token, email, tier, and expires_at fields only at the verified atomic replacement; create no account.json and report that a fresh sworn login is required.",
    "Use the original composite as the recovery source before replacement; converge every declared interruption point and leave no temporary or permanent token-bearing recovery material after replacement.",
    "Preserve providers, permissions, and canonical provider environment precedence throughout migration and later record-specific writes; require S05 to consume legacy webhook_url before the provider rewrite and fail closed on any unclaimed field.",
    "Make sworn doctor report absent, legacy, migrated, conflicting, corrupt, and unsupported layouts without printing field values; make its fix path call the same migration routine used by account-aware commands.",
    "Keep provider-only readers and commands independent of account migration and account-record validity."
  ],
  "out_of_scope": [
    "Device login, issuer validation, account output, and logout gestures belong to S02-authoritative-account-session.",
    "Model routing and proxy removal belong to S03-direct-routing-only.",
    "Legacy webhook_url migration and notifications.json belong to S05-independent-webhook-consent.",
    "OS keyrings, hardware-backed secret storage, and a general encrypted state database are not introduced."
  ],
  "covers_needs": ["N-01", "N-08", "N-09"],
  "effort_complexity": {
    "effort": "high",
    "complexity": "high",
    "quadrant": "beast",
    "rationale": "Two live secret formats, crash ordering, compatibility, cache freshness, permissions, and recovery behavior create a wide and failure-sensitive migration surface.",
    "confirmed_by_implementer": false
  },
  "risks": [
    {
      "id": "R-01",
      "description": "A crash during provider-record replacement loses a provider key or strands an ambiguous temporary record.",
      "mitigation": "Use named kill points before temporary write, after write, after sync, after re-read verification, and after atomic rename; every fixture must converge on retry with the provider map unchanged and no unclaimed field discarded."
    },
    {
      "id": "R-02",
      "description": "A migration backup or quarantine becomes a permanent second copy of the issuer-less account token.",
      "mitigation": "Create no backup or quarantine: the source composite remains authoritative before rename, the verified C-01 record is authoritative after rename, and no temporary file may remain after completion."
    },
    {
      "id": "R-03",
      "description": "A new binary writes a provider record an older binary can no longer read.",
      "mitigation": "Keep credentials.json and the providers field stable; version is additive and an old-reader compatibility test unmarshals the migrated provider record."
    }
  ],
  "acceptance_criteria": [
    {
      "id": "AC-01",
      "text": "WHEN the built binary runs sworn doctor --fix against a valid unversioned composite containing only a providers map, token, email, tier, and expires_at THE SYSTEM SHALL exit 2, write C-01 with mode 0600, preserve the complete providers map by deep equality, omit the four issuer-less account fields, create no account.json, leave no temporary file, print no field value, and state that fresh sworn login is required (C-01, C-02, N-01, N-08).",
      "ears_pattern": "event-driven",
      "test_refs": ["cmd/sworn/credentials_integration_test.go:TestAccountSplitMigrationRealFilesystem"]
    },
    {
      "id": "AC-02",
      "text": "WHEN the migration test hook returns its sentinel error at before-temp-create, after-temp-write, after-temp-sync, after-temp-verify, or after-rename THE SYSTEM SHALL, for every pre-rename hook, leave credentials.json at the original SHA-256 and, for after-rename, leave it at the uninterrupted migration's canonical SHA-256; after the hook is removed, one retry SHALL produce that canonical SHA-256, no account.json, and zero .credentials-*.tmp files (C-01, C-02, N-01).",
      "ears_pattern": "event-driven",
      "test_refs": ["internal/credentials/store_test.go:TestAccountSplitMigrationConvergesAcrossKillPoints"]
    },
    {
      "id": "AC-03",
      "text": "IF the legacy composite is malformed, C-01 or C-02 has an unsupported version, or account.json already exists while legacy account fields remain THEN THE SYSTEM SHALL exit 1, print exactly one of 'Credential migration blocked: malformed credentials; run sworn doctor', 'Credential migration blocked: unsupported record version; run sworn doctor', or 'Credential migration blocked: conflicting account state; run sworn doctor', preserve SHA-256 hashes of every existing record, create no temporary file, and omit the fixture API-key, token, email, webhook path, and signing-secret canaries from stdout and stderr (C-01, C-02, N-01, N-09).",
      "ears_pattern": "unwanted-behaviour",
      "test_refs": ["cmd/sworn/doctor_test.go:TestCredentialMigrationConflictFailsClosed"]
    },
    {
      "id": "AC-04",
      "text": "IF unversioned credentials.json contains webhook_url THE SYSTEM SHALL invoke the C-05 owning migration before C-01 replacement; IF any top-level key other than providers, token, email, tier, expires_at, or webhook_url remains after recognized migrations THEN THE SYSTEM SHALL exit 1 with exactly 'Credential migration blocked: unclaimed legacy field; run sworn doctor', retain the original credentials.json SHA-256, create neither account.json nor a temporary file, and print neither the key name nor any fixture value (C-01, C-05, N-01, N-09).",
      "ears_pattern": "unwanted-behaviour",
      "test_refs": ["cmd/sworn/credentials_integration_test.go:TestProviderRewriteRequiresOwnedLegacyFieldMigration"]
    },
    {
      "id": "AC-05",
      "text": "WHEN one process reads provider key provider-key-a, a second process atomically replaces C-01 with provider-key-b, and the first process reads again THE SYSTEM SHALL return provider-key-b on the second read and preserve the pre-test SHA-256 of account.json throughout (C-01, C-02, N-01).",
      "ears_pattern": "event-driven",
      "test_refs": ["internal/model/credentials_test.go:TestProviderStoreIsAccountIndependentAndFresh"]
    },
    {
      "id": "AC-06",
      "text": "WHEN the migrated credentials.json bytes are decoded by the legacy compatibility struct containing only Providers map[string]string THE SYSTEM SHALL produce a map deeply equal to the source providers fixture, and raw top-level inspection SHALL find version and providers but no token, email, tier, expires_at, or webhook_url (C-01, N-01, N-09).",
      "ears_pattern": "event-driven",
      "test_refs": ["cmd/sworn/credentials_integration_test.go:TestVersionedProviderCredentialMigrationRealFilesystem"]
    },
    {
      "id": "AC-07",
      "text": "WHILE account.json is malformed JSON or has version=2, WHEN the built binary resolves an openai-completions/test-model provider using provider-key-canary from C-01 or the canonical environment override THE SYSTEM SHALL select that provider key, exit with the provider fixture's expected result, leave both record SHA-256 values unchanged, and make zero calls to the migration routine (C-01, C-02, N-01, N-08).",
      "ears_pattern": "state-driven",
      "test_refs": ["cmd/sworn/credentials_integration_test.go:TestCorruptAccountDoesNotDisableProviderOnlyResolution"]
    }
  ]
}
