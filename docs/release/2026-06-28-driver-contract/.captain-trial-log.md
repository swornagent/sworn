| Slice | Date | §2 count | §6 count | Files | Pins (mech/mem/esc) | Notes |
|-------|------|----------|----------|-------|----------------------|-------|
| S01-driver-contract | 2026-07-03 | 4 | 0 | 6 | 5 (3/2/0) | Live-repo grounding checks (AssertWorktree on this project's own `.git`-file worktree shape, GH issue citation fetch) caught two real defects a text-only read would have missed — worth doing even with no §6 questions to anchor on. |
| S02-claude-subprocess-driver | 2026-07-03 | 9 | 0 | 7 | 4 (2/1/1) | Reading sibling specs S06/S07 alongside S02's design (not just S02 in isolation) surfaced a real cross-slice gap: S07's own AC-03 text already assumes an error-kind distinction S02's new vocabulary quietly drops — the kind of fidelity gap no single slice's own ACs can catch. |
| S03-codex-subprocess-driver | 2026-07-06 | 7 | 2 | 4 | 3 (2/1/0)* | Diffing S03's AC-02 against verified sibling S02's AC-03 surfaced a Rule-7 fresh-context gap the implementer's own §6 never raised; Brad's live codex CLI docs resolved it same-session (`--ephemeral` = codex's `--no-session-persistence` equivalent, Role==RoleVerifier-scoped) and flipped NEEDS_COACH -> PROCEED, plus surfaced a free envelope-shape correction (`turn.completed` has no `model`/`duration_ms`). Separately, the implementer's own escalate-flagged ErrKind question was already answered by S02's binding cross-driver contract in memory. *pin counts reflect post-resolution tags. |
| S13-regress-go-module-cwd | 2026-07-10 | 3 | 0 | 2 | 3 (2/1/0) | Reading the OTHER package's consumer (cmd/sworn/regress_test.go runs the real RunRegress against no-go.mod temp dirs) turned "full sweep is mandatory" from a slogan into a verified fact: the Go suite there flips FAIL→Skipped after this slice, and only reading its assertions (`exit != 2`) proved the flip is tolerated — a behaviour change in a package the design never touches. |
| S04-inprocess-oai-driver | 2026-07-06 | 7 | 2 | 3 | 6 (3/0/3) | Tracing the `ErrKind` value FORWARD to its consumer (slice.go fail-fast + the still-unplanned S06 rewire) — not just checking it against the sibling vocab — exposed that the fail-fast currently keys off the returned *error*, so a driver that emits richer `ErrKind` strings plants an untracked terminal-set requirement on S06. And reading BOTH backing clients for the two-identity design (D1) caught that the S27 content-fix is on `oai.go` but not `openai_responses.go` (out of scope), leaving AC-03 untested for the second identity. Two cross-slice/scope gaps a spec-vs-design-only read misses. |
| S05-driver-registry | 2026-07-10 | 8 | 4 | 9 | 5 (3/1/1) | Tracing AC-05's ViaProxy visibility FORWARD to the dispatch leg it advertises exposed that the registry's resolved drivers dispatch via proxy-blind `model.NewClient` while the visibility condition lives only in `FromEnv` — enumeration would advertise routing the post-S06 loop cannot take, and no slice's spec owns closing the gap (S06 R-01..R-03 checked). Third release instance of the same lesson: the load-bearing pin comes from walking a value to its cross-slice consumer, never from the slice's own ACs. |
| S06-loop-dispatch-rewire | 2026-07-10 | 10 | 4 | 7+14 | 6 (3/2/1) | The one inaccuracy in an otherwise fully-verified design was found by reading the cited error's actual fmt.Errorf — the registry role-arm names driver/role/alternatives but not the model AC-02 requires; verify a design's paraphrase of an error message at the format-string level, never trust the prose. |
| S07-scheduler-failfast | 2026-07-10 | 4 | 1 | 8 | 4 (2/1/1) | Every one of ~20 spot-checked line/quote citations against live code held exactly — the one real defect (a test-coverage claim: "FailureCascade asserts the dependent is skipped") was found only by reading the full cited test function rather than trusting the paraphrase, same lesson as S06's row; the escalate pin is the design's own self-flagged AC-01-vs-S06-precedent conflict, correctly surfaced rather than silently resolved. |
| S08-honest-cost-telemetry | 2026-07-10 | 3 | 4 | 24 | 6 (2/2/2) | Every code citation checked (deleted `computeAgenticCost`, all four pricing-lookup implementations, `agent.Run`'s exactly-two call sites, `claude.go`/`codex.go`'s literal `costSource()` strings, all five `slice.go` Dispatch sites) held exactly — the design is unusually well-grounded. Both escalate pins are the design's own self-flagged financial-honesty judgment calls (D1/D2's unverified-against-live-binary subscription inference; AC-02's unreachable "provider" branch) with no `status.json.design_decisions` recorded yet — same missing-field gap [[project_driver_contract_recut]] already flagged on sibling S04, now recurring on S08. |
| S14-blocked-terminal | 2026-07-11 | 6 | 0 | 9 | 5 (3/2/0) | The critical pin came not from the design's cited symbols (all ~15 held exactly: StatusBlocked, Decide(Blocked)->Halt, three worker sentinel sites, TrackBlocked/invariant-2, the exact RunParallel format string, all nine protected AC-06 tests) but from following the design's ONE unchecked call one level deeper — supervisor.Release coerces "blocked" to "done"; verify the semantics of every function a design passes a new argument to, not just the symbols it quotes. |
