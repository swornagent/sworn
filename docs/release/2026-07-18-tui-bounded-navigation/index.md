---
title: 'Release board — 2026-07-18-tui-bounded-navigation'
description: 'Rendered view of board.json + slice records. Generated by `sworn render 2026-07-18-tui-bounded-navigation` — do not hand-edit; edit board.json and re-render.'
---

# Release board: 2026-07-18-tui-bounded-navigation

> Generated by `sworn render`. This file is a deterministic view of `board.json`
> plus each slice's `spec.json`/`status.json` — never hand-authored.

## Tracks

| Track | Slices | depends_on | State |
|-------|--------|------------|-------|
| `T1-tui-bounded-navigation` | `S01-tui-bounded-navigation` | — | in_progress |

## Slices

| Slice | Track | Outcome | State | E×C |
|-------|-------|---------|-------|-----|
| `S01-tui-bounded-navigation` | `T1-tui-bounded-navigation` | A SwornAgent operator can launch the TUI in a project with many releases, load only the recent catalog depth they need, scroll the release and slice panes inside the current terminal height, resize without losing selection, and move between panes with Right/Left or Enter/Esc. | failed_verification | puzzle |

## Touchpoint matrix

Every planned-write file × track. A file marked under two tracks is a
collision — disjointness is what licenses tracks to run independently.

| File | T1-tui-bounded-navigation |
|------|----|
| `internal/board/discovery.go` | ✓ |
| `internal/board/discovery_test.go` | ✓ |
| `internal/tui/board.go` | ✓ |
| `internal/tui/model.go` | ✓ |
| `internal/tui/releases.go` | ✓ |
| `internal/tui/styles.go` | ✓ |
| `internal/tui/tui.go` | ✓ |
| `internal/tui/tui_test.go` | ✓ |

## Dependency graph

```
T1-tui-bounded-navigation  (root)
```
