---
title: 'Release board — 2026-07-11-loop-operability'
description: 'Rendered view of board.json + slice records. Generated by `sworn render 2026-07-11-loop-operability` — do not hand-edit; edit board.json and re-render.'
---

# Release board: 2026-07-11-loop-operability

> Generated by `sworn render`. This file is a deterministic view of `board.json`
> plus each slice's `spec.json`/`status.json` — never hand-authored.

## Tracks

| Track | Slices | depends_on | State |
|-------|--------|------------|-------|
| `T1-conformance` | `S01-spec-json-read-conformance`, `S02-model-response-structured` | — | in_progress |
| `T2-xai-driver` | `S03-xai-driver` | — | in_progress |

## Slices

| Slice | Track | Outcome | State | E×C |
|-------|-------|---------|-------|-----|
| `S01-spec-json-read-conformance` | `T1-conformance` | Every site in sworn that reads a slice's machine contract reads spec.json (spec-v1) as authoritative, with spec.md used only as a legacy fallback for pre-spec-v1 slices — matching the ears.go/reqverify.go pattern (ADR-0009). Most importantly, the engine's implement leg (the autonomous loop's RunSlice → implement.Run path) reads spec.json, so `sworn run --parallel` can implement a spec-v1 release instead of hard-failing on a missing spec.md (sworn#97). A spec.json-only release (what /plan-release produces) drives the loop end-to-end. | planned | grind |
| `S02-model-response-structured` | `T1-conformance` | The loop gates that consume a MODEL response accept schema-constrained structured output instead of scraping model prose in a model-specific format — so a capable model that does not reproduce the exact prose shape (as Grok did not) no longer fails the gate. Specifically the design-TL;DR gate (which required §1-§6 prose headers) and the reqverify Definition-of-Ready gate (which scraped a `## RESULTS` prose section) consume structured JSON validated against a schema; the schema is the contract, not the prose shape. This makes `sworn run` model-portable — the ADR-0011 structured-output keystone extended to the sites it missed. | planned | puzzle |
| `S03-xai-driver` | `T2-xai-driver` | `xai/` is a first-class driver prefix: `xai/grok-4.5` (and other xAI models) resolves natively through the driver registry to xAI's OpenAI-compatible API using XAI_API_KEY, declaring implementer/verifier/captain roles like the other in-process prefixes — so the loop can be driven on Grok directly (`--implementer-model xai/grok-4.5`) instead of only via `openrouter/x-ai/grok-4.5`. `sworn capabilities` lists the xai driver as available when XAI_API_KEY is present. | verified | grind |

## Touchpoint matrix

Every planned-write file × track. A file marked under two tracks is a
collision — disjointness is what licenses tracks to run independently.

| File | T1-conformance | T2-xai-driver |
|------|----|----|
| `internal/design/` | ✓ |  |
| `internal/design/design_test.go` | ✓ |  |
| `internal/gate/coverage.go` | ✓ |  |
| `internal/gate/trace.go` | ✓ |  |
| `internal/implement/implement.go` | ✓ |  |
| `internal/implement/implement_test.go` | ✓ |  |
| `internal/implement/proof_record.go` | ✓ |  |
| `internal/implement/spec_record.go` | ✓ |  |
| `internal/model/` | ✓ | ✓ |
| `internal/reqverify/reqverify.go` | ✓ |  |
| `internal/reqverify/reqverify_test.go` | ✓ |  |
| `internal/rtm/rtm.go` | ✓ |  |
| `internal/run/run.go` | ✓ |  |
| `internal/scheduler/worker.go` | ✓ |  |
| `internal/specquality/specquality.go` | ✓ |  |
| `cmd/sworn/capabilities.go` |  | ✓ |
| `cmd/sworn/models.go` |  | ✓ |
| `internal/driver/registry/` |  | ✓ |
| `internal/driver/registry/registry_test.go` |  | ✓ |
| `internal/model/catalog.go` |  | ✓ |

## Dependency graph

```
T1-conformance  (root)
T2-xai-driver  (root)
```
