---
title: 'Release board — 2026-07-11-contract-edge-gates'
description: 'Rendered view of board.json + slice records. Generated by `sworn render 2026-07-11-contract-edge-gates` — do not hand-edit; edit board.json and re-render.'
---

# Release board: 2026-07-11-contract-edge-gates

> Generated by `sworn render`. This file is a deterministic view of `board.json`
> plus each slice's `spec.json`/`status.json` — never hand-authored.

## Tracks

| Track | Slices | depends_on | State |
|-------|--------|------------|-------|
| `T1-lint-contracts` | `S01-lint-contracts-registry`, `S02-lint-contracts-mock-parity` | — | planned |
| `T2-assemble` | `S03-assemble-command` | — | planned |

## Slices

| Slice | Track | Outcome | State | E×C |
|-------|-------|---------|-------|-----|
| `S01-lint-contracts-registry` | `T1-lint-contracts` | `sworn lint contracts <release>` grades a release's contracts.json registry, fail-closed: it validates every record against the vendored contracts-v1 schema, flags any spec.json in-scope/AC that names a wire-level artefact (header, endpoint path, env var, schema-version, storage key) with no registry entry, verifies each contract's live_test resolves to a real test, and rejects a surface with two owners or an owner slice whose touchpoints cannot plausibly contain the surface. Absent contracts.json is an advisory warning (exit 0) during the skew window. It follows the existing `sworn lint` subcommand conventions (ac/trace/deps/coverage/design/mock). | planned | grind |
| `S02-lint-contracts-mock-parity` | `T1-lint-contracts` | `sworn lint contracts` gains the Rule-10 mock-parity checks: when a contract names `fixtures`, a consumer slice may mock that registered boundary only with the owner's recorded fixtures — the gate FAILs if the fixture file is missing or older than the owner's last production-code commit touching the surface, and FAILs if a consumer's tests mock the boundary without importing the fixture path AND without at least one unmocked in-process round-trip. This closes the seam where a mock encodes the consumer's own wrong assumption of the boundary. | planned | puzzle |
| `S03-assemble-command` | `T2-assemble` | `sworn assemble <release>` is the machine half of the Rule-10 assembly stage: it brings up the assembled release's stack from the release worktree, runs the release's deferred end-to-end set no-mock, serially, with verified teardown, and emits docs/release/<release>/assembly-proof.json (validated against the vendored assembly-proof-v1). It exits non-zero on any non-excepted failure. /merge-release (baton side, already landed) reads that artefact, flipping the assembly gate from advisory to enforced. | planned | beast |

## Touchpoint matrix

Every planned-write file × track. A file marked under two tracks is a
collision — disjointness is what licenses tracks to run independently.

| File | T1-lint-contracts | T2-assemble |
|------|----|----|
| `cmd/sworn/lint.go` | ✓ |  |
| `cmd/sworn/lint_test.go` | ✓ |  |
| `internal/lint/contracts.go` | ✓ |  |
| `internal/lint/contracts_mock_parity_test.go` | ✓ |  |
| `internal/lint/contracts_test.go` | ✓ |  |
| `internal/lint/testdata/contracts/` | ✓ |  |
| `cmd/sworn/assemble.go` |  | ✓ |
| `cmd/sworn/assemble_test.go` |  | ✓ |
| `internal/assemble/` |  | ✓ |
| `internal/assemble/proof.go` |  | ✓ |
| `internal/assemble/testdata/` |  | ✓ |

## Dependency graph

```
T1-lint-contracts  (root)
T2-assemble  (root)
```
