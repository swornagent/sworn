{
  "$schema": "https://baton.sawy3r.net/schemas/spec-v1.json",
  "slice_id": "S02-lint-contracts-mock-parity",
  "release": "2026-07-11-contract-edge-gates",
  "track": "T1-lint-contracts",
  "user_outcome": "`sworn lint contracts` gains the Rule-10 mock-parity checks: when a contract names `fixtures`, a consumer slice may mock that registered boundary only with the owner's recorded fixtures — the gate FAILs if the fixture file is missing or older than the owner's last production-code commit touching the surface, and FAILs if a consumer's tests mock the boundary without importing the fixture path AND without at least one unmocked in-process round-trip. This closes the seam where a mock encodes the consumer's own wrong assumption of the boundary.",
  "rationale": "W3 mock-parity half (proposal Rec 2, baton customer-journey-validation.md sub-rule). Builds on S01's contracts subcommand (same internal/lint/contracts.go entry, added check family). The freshness invariant needs a git-timestamp comparison: fixture file mtime-in-git vs the owner slice's last production-code commit touching the surface (reuse the same git-log machinery the mock lint / deps gates already use). Consumer-mock detection is grep-level to start (per the proposal: 'grep-level is sufficient to start') — detect a test that mocks a registered boundary and assert it imports from the fixture path or contains an unmocked round-trip. Serial after S01 in T1 (same file surface).",
  "covers_needs": ["N-01"],
  "in_scope": [
    "Fixture-freshness check: when a contract names `fixtures`, FAIL if the fixture file does not exist OR is older (in git history) than the owner slice's last production-code commit touching the surface",
    "Consumer mock-parity: FAIL if a consumer slice's tests mock a registered boundary WITHOUT importing from the contract's fixture path AND WITHOUT at least one unmocked in-process round-trip against the real handler (grep-level detection to start)",
    "Wire the mock-parity checks into `sworn lint contracts` as an additional fail-closed check family (extends S01's internal/lint/contracts.go)",
    "Test fixtures reproducing the fired seam-2 case: a consumer mock that loads no owner-recorded fixture → FAIL"
  ],
  "out_of_scope": [
    "The registry-grading checks (S01 owns schema-validate / wire-ref / live_test / ownership)",
    "sworn assemble (T2)",
    "Deep AST-level mock detection — grep-level is the ratified starting point (proposal Rec 2); a tighter detector is a future follow-up",
    "The existing `sworn lint mock` subcommand — mock-parity here is contracts-registry-scoped, distinct from that gate"
  ],
  "effort_complexity": {
    "effort": "low",
    "complexity": "high",
    "quadrant": "puzzle",
    "rationale": "Small surface but the fixture-freshness git-timestamp comparison and the mock-vs-fixture-import detection are fiddly correctness (false positives erode the gate); depth over breadth. Re-assess at design_review.",
    "confirmed_by_implementer": false
  },
  "touchpoints": [
    "internal/lint/contracts.go",
    "internal/lint/contracts_mock_parity_test.go",
    "internal/lint/testdata/contracts/"
  ],
  "risks": [
    {
      "id": "R-01",
      "description": "Grep-level mock detection misses a mock (false negative) or over-fires on an unrelated test that names the boundary (false positive).",
      "mitigation": "Start grep-level per the proposal's explicit ratification; the fired seam-2 fixture is the positive case (must FAIL); a well-formed consumer with an unmocked round-trip is the negative (must PASS). Tighter detection tracked as a follow-up, not blocking."
    },
    {
      "id": "R-02",
      "description": "Fixture-freshness git-timestamp comparison is wrong across worktrees / rebased history.",
      "mitigation": "Reuse the git-log machinery the deps/mock gates already use (same commit-resolution path); test against a fixture with a known owner-commit-after-fixture-write ordering."
    }
  ],
  "acceptance_criteria": [
    {
      "id": "AC-01",
      "text": "When a contract names `fixtures` and the fixture file is missing OR older in git history than the owner slice's last production-code commit touching the surface, `sworn lint contracts` SHALL exit non-zero naming the contract, the stale/missing fixture, and the owner commit.",
      "ears_pattern": "event-driven"
    },
    {
      "id": "AC-02",
      "text": "When a consumer slice's tests mock a boundary registered in contracts.json without importing from the contract's fixture path AND without at least one unmocked in-process round-trip against the real handler, `sworn lint contracts` SHALL exit non-zero naming the consumer slice and the boundary — proven against a fired seam-2 fixture (a mock loading no owner-recorded fixture → FAIL).",
      "ears_pattern": "event-driven"
    },
    {
      "id": "AC-03",
      "text": "`sworn lint contracts` SHALL exit 0 for a consumer that mocks a registered boundary but does import the owner fixture, or that includes an unmocked in-process round-trip (the escape hatch), and `go test ./internal/lint/...` SHALL pass.",
      "ears_pattern": "ubiquitous"
    }
  ]
}
