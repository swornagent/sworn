{
  "$schema": "https://baton.sawy3r.net/schemas/spec-v1.json",
  "slice_id": "S01-endpoint-registry-config",
  "release": "2026-07-14-local-cloud-providers",
  "track": "T1-provider-runtime",
  "user_outcome": "A SwornAgent operator selects an Ollama Cloud or supported local provider prefix, optionally sets providers.<prefix>.base_url in config.json, and the sworn verify entry point reaches the expected endpoint through one declared provider table without leaking credentials to keyless services.",
  "rationale": "This is the endpoint foundation for sworn#15. It replaces repeated OAI-compatible factory arms with explicit data while retaining inspectable prefix resolution. The table covers existing chat/completions providers and the new cloud/local providers; native and Responses drivers remain explicit because their wire formats genuinely differ. Config endpoint overrides become durable and visible, while the obsolete SWORN_<PROVIDER>_BASE_URL path is warned about and ignored. The slice owns contracts C-01, C-02, C-04, and C-05.",
  "touchpoints": [
    "internal/config/config.go",
    "internal/config/config_test.go",
    "internal/model/endpoint.go",
    "internal/model/endpoint_test.go",
    "internal/model/provider.go",
    "internal/model/provider_test.go",
    "internal/model/provider_env_test.go",
    "internal/model/credentials.go",
    "internal/model/credentials_test.go",
    "internal/model/config.go",
    "internal/model/oai_test.go",
    "cmd/sworn/provider_config.go",
    "cmd/sworn/provider_config_test.go",
    "cmd/sworn/verify.go",
    "cmd/sworn/verify_test.go",
    "cmd/sworn/doctor.go",
    "cmd/sworn/doctor_test.go"
  ],
  "in_scope": [
    "Create a declared OAI chat endpoint descriptor table whose generic constructor supplies prefix, default base URL, credential provider, and existing structured mode; retain openai/ Responses, native anthropic/google/vertex/bedrock/azure/oci, claude-cli/, and ollama-native/ as explicit non-table dispatch paths.",
    "Migrate existing OAI chat prefixes into the table without behavioural drift: openai-completions, deepseek, groq, mistral, openrouter, xai, cloudflare, and github retain their current default URL, credential, full model-subpath, and structured-mode semantics.",
    "Add ollama-cloud/ (https://ollama.com/v1, credential provider ollama-cloud), ollama/ (http://localhost:11434/v1, keyless), llamacpp/ (http://localhost:8080/v1, keyless), lmstudio/ (http://localhost:1234/v1, keyless), vllm/ (http://localhost:8000/v1, keyless), and localai/ (http://localhost:8080/v1, keyless) as OAI chat descriptors with structured mode unsupported until S04/S05 discover and apply per-model capability.",
    "Move the existing native Ollama /api/chat utility client to ollama-native/ with default http://localhost:11434; document and test that ollama/ now has one OAI-compatible meaning across utility and later loop dispatch paths.",
    "Add Config.Providers as a map keyed by canonical prefix whose value is a provider object containing optional base_url; merge it into model.ProviderConfig through one cmd/sworn adapter used by verify now and the remaining commands in S02.",
    "Validate config provider keys against the declared endpoint/native prefix set and base_url as an absolute http(s) URL with no userinfo or fragment; reject unknown fields inside a provider object, and reject non-HTTPS overrides for descriptors that attach a credential.",
    "Add ollama-cloud -> OLLAMA_API_KEY to canonical credential resolution. Ensure ollama/, llamacpp/, lmstudio/, vllm/, and localai/ never attach any credential even when OLLAMA_API_KEY or another provider key is present.",
    "Remove runtime use of SWORN_<PROVIDER>_BASE_URL. When one is set, emit a value-free warning naming the variable and providers.<prefix>.base_url, ignore it, and resolve from config then descriptor default. Extend sworn doctor with the same migration finding.",
    "Preserve the no-key/no-payload logging contract and full-model-name pass-through for model IDs containing additional slashes."
  ],
  "out_of_scope": [
    "Loop registry registration and keyless /models availability probes are S02-keyless-loop-dispatch.",
    "Dialect probing, generated dialect records, and OAI request adaptation are S03-endpoint-dialect-conformance.",
    "Live workflow matrix and external provider evidence are S04-live-provider-matrix, except the opt-in owner tests required by contracts C-02/C-04/C-05.",
    "Per-model capability discovery and role eligibility remain S04-provider-registry and S05-capability-eligibility in 2026-07-11-contract-edge-gates.",
    "Adding credentials, capability claims, pricing data, models, or role selection inside config provider objects is forbidden."
  ],
  "covers_needs": [
    "N-01"
  ],
  "effort_complexity": {
    "effort": "high",
    "complexity": "high",
    "quadrant": "beast",
    "rationale": "The implementation is a broad public-contract migration across config, credential, utility dispatch, doctor, and provider construction. The correct structure is known, but preserving every existing provider while changing Ollama prefix semantics and eliminating an override path creates a wide blast radius.",
    "confirmed_by_implementer": false
  },
  "risks": [
    {
      "id": "R-01",
      "description": "The endpoint table becomes a second registry whose prefix, credential, or wire meaning drifts from loop dispatch.",
      "mitigation": "S01 makes the table the construction authority consumed by model.NewClient; S02's registry derives its OAI prefix groups from declared descriptors and tests registry/NewClient agreement for every row. No second hand-written provider list is allowed."
    },
    {
      "id": "R-02",
      "description": "OLLAMA_API_KEY is accidentally attached to local ollama/ requests or a keyed endpoint is overridden to plaintext HTTP.",
      "mitigation": "Credential attachment is descriptor-owned, local descriptors declare none, config validation rejects non-HTTPS overrides for credentialed descriptors, and transport tests assert Authorization absence/presence without reflecting the secret."
    },
    {
      "id": "R-03",
      "description": "Removing BASE_URL environment overrides silently routes an operator to a different endpoint.",
      "mitigation": "Every removed variable produces a value-free runtime warning and a doctor finding with the exact replacement config path; tests set both env and config and prove config/default wins."
    },
    {
      "id": "R-04",
      "description": "The ollama/ migration makes utility and loop paths disagree or silently removes native Ollama access.",
      "mitigation": "Command-level tests prove ollama/ uses /v1/chat/completions and ollama-native/ uses /api/chat, and provider/registry consistency tests prevent role-dependent prefix meaning."
    }
  ],
  "acceptance_criteria": [
    {
      "id": "AC-01",
      "text": "WHEN sworn verify resolves a configured new provider model through a temporary config.json containing providers.<prefix>.base_url THE SYSTEM SHALL send the model request to that absolute override using the descriptor's /chat/completions wire, preserve the model substring after the first slash verbatim, and return the verifier verdict/exit-code pair from the command integration point (C-01, C-04, N-01).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "cmd/sworn/verify_test.go:TestVerifyConfiguredProviderEndpointReachability"
      ]
    },
    {
      "id": "AC-02",
      "text": "THE SYSTEM SHALL construct openai-completions/, deepseek/, groq/, mistral/, openrouter/, xai/, cloudflare/, github/, ollama-cloud/, ollama/, llamacpp/, lmstudio/, vllm/, and localai/ from one declared OAI endpoint table, and adding another OAI-compatible prefix SHALL require one descriptor-row change with no provider switch, credential switch, or registry-prefix-list edit (N-01).",
      "ears_pattern": "ubiquitous",
      "test_refs": [
        "internal/model/endpoint_test.go:TestDeclaredOAIEndpointsConstruct",
        "internal/model/endpoint_test.go:TestEndpointTableIsPrefixAuthority"
      ]
    },
    {
      "id": "AC-03",
      "text": "WHEN model.NewClient resolves ollama/<model> or ollama-native/<model> THE SYSTEM SHALL return respectively an OAI chat client with default base http://localhost:11434/v1 or the native Ollama client with default host http://localhost:11434, and command transport tests SHALL observe /v1/chat/completions versus /api/chat without one model ID changing meaning by role (N-01).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/model/provider_test.go:TestOllamaPrefixMigration",
        "cmd/sworn/verify_test.go:TestVerifyOllamaAndNativeReachability"
      ]
    },
    {
      "id": "AC-04",
      "text": "IF config.json contains an unknown provider key, an unknown field inside a provider object, a relative/non-HTTP(S)/userinfo/fragment base_url, or a plaintext HTTP override for a credentialed descriptor THEN config Load/Validate/Save and sworn verify SHALL fail closed before dispatch and name the invalid config path without printing credentials (C-01, N-01).",
      "ears_pattern": "unwanted-behaviour",
      "test_refs": [
        "internal/config/config_test.go:TestProviderEndpointValidation",
        "cmd/sworn/verify_test.go:TestVerifyInvalidProviderConfigFailsBeforeDispatch"
      ]
    },
    {
      "id": "AC-05",
      "text": "WHEN OLLAMA_API_KEY is available through the canonical environment or credentials.json THE SYSTEM SHALL attach it as a Bearer credential only for ollama-cloud/ over HTTPS and SHALL send no Authorization header for ollama/, llamacpp/, lmstudio/, vllm/, or localai/ (C-02, C-04, C-05, N-01).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/model/credentials_test.go:TestOllamaCloudCanonicalCredential",
        "internal/model/endpoint_test.go:TestLocalEndpointsNeverReceiveCredentials",
        "internal/model/ollama_cloud_live_test.go:TestLiveOllamaCloudChat"
      ]
    },
    {
      "id": "AC-06",
      "text": "WHEN any removed SWORN_<PROVIDER>_BASE_URL variable is set THE SYSTEM SHALL ignore its value, emit exactly one value-free migration warning per command naming providers.<prefix>.base_url, continue from config/default, and sworn doctor SHALL report the same migration without displaying the value (N-01).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/model/oai_test.go:TestLegacyBaseURLWarnedAndIgnored",
        "cmd/sworn/doctor_test.go:TestDoctorReportsRemovedProviderBaseURL"
      ]
    },
    {
      "id": "AC-07",
      "text": "THE SYSTEM SHALL keep all pre-existing provider constructors and tests behaviourally compatible except for the ratified ollama/ migration, and go test ./internal/config/... ./internal/model/... ./cmd/sworn/... plus go vet ./... and gofmt -l SHALL pass (N-01).",
      "ears_pattern": "ubiquitous",
      "test_refs": [
        "internal/model/provider_test.go:TestNewClientOAICompatProviders",
        "internal/model/provider_env_test.go:TestProviderConfigFromEnv"
      ]
    }
  ]
}
