{
  "$schema": "https://baton.sawy3r.net/schemas/spec-v1.json",
  "slice_id": "S04-live-provider-matrix",
  "release": "2026-07-14-local-cloud-providers",
  "track": "T2-live-matrix",
  "user_outcome": "A SwornAgent maintainer opens the nightly or manually dispatched live workflow and sees a secret-safe RUN, FAIL, or reasoned SKIP result for every declared OpenAI-compatible endpoint, with Ollama Cloud and the existing OpenAI hosted canary required to run and dialect drift surfaced as a reviewable artifact rather than an automatic repository mutation.",
  "rationale": "Live provider behaviour cannot be established by deterministic mocks, but unavailable credentials and local daemons must not disappear from coverage. This slice makes the declared endpoint set and every omission machine-countable, keeps paid and non-deterministic calls outside the pull-request gate, and turns regenerated dialect drift into an explicit failed workflow with review artifacts. It consumes C-01 through C-06 and owns C-07.",
  "touchpoints": [
    ".github/workflows/live.yml",
    "scripts/live-endpoint-row.sh",
    "scripts/live-endpoint-matrix.sh",
    "internal/liveconformance/matrix.go",
    "internal/liveconformance/matrix_test.go",
    "internal/liveconformance/schemas/endpoint-conformance-matrix-v1.json",
    "internal/model/live_matrix_test.go"
  ],
  "in_scope": [
    "Replace the single-model live workflow body with a declared endpoint matrix containing every OpenAI-compatible descriptor delivered by S01: existing hosted endpoints plus ollama-cloud, ollama, llamacpp, lmstudio, vllm, and localai. A deterministic test compares workflow provider identifiers with the endpoint descriptor table so either side drifting alone fails.",
    "Keep `.github/workflows/live.yml` callable only by nightly schedule and workflow_dispatch, never pull_request or merge_group. Preserve the existing real-model contract coverage while adding endpoint conformance rows.",
    "Treat `ollama-cloud` and the existing `openai` hosted canary as mandatory nightly rows. A missing required credential, model identifier, malformed configuration, unreachable endpoint, failed probe, or missing result makes that row FAIL and the workflow non-zero; mandatory rows can never become SKIP.",
    "Represent each configured local or optional hosted endpoint as RUN, FAIL, or SKIP with a closed reason-code vocabulary. A row may SKIP only when it is declared optional and its required runner/base URL/model/credential is unavailable; probe rejection of an individual optional feature remains a successful RUN observation, while transport/auth/protocol/schema failure is FAIL.",
    "Provide workflow_dispatch inputs sufficient to configure a real local Ollama endpoint/model and the broader local matrix on an appropriate runner. Default GitHub-hosted execution emits explicit SKIP rows for unavailable local services rather than attempting localhost by assumption. Release proof must preserve either a real local Ollama RUN artifact or its explicit SKIP reason.",
    "Write one endpoint-conformance-matrix-v1 aggregate containing exactly one row per declared endpoint, stable provider ordering, status, closed reason code, safe probe-model identity, and relative dialect-record artifact reference. Reject duplicate, missing, unknown, malformed, or secret-bearing rows and fail when no mandatory row executed.",
    "For every RUN row, invoke the built `sworn endpoint-conformance probe` command and retain its schema-valid raw observation as a per-provider artifact. Aggregate only safe metadata; never include credentials, Authorization values, endpoint userinfo/query strings, request/response bodies, prompts, or model output.",
    "Regenerate the normalized dialect candidate from all successful observations and invoke `sworn endpoint-conformance check`. Equality stays green; drift fails the workflow, uploads the raw observations and candidate normalized table, and leaves the checked-out repository unchanged. The workflow never commits, pushes, comments, or opens a pull request.",
    "Prove enforcement sensitivity by exercising a fixture in which a mandatory row is rewritten to SKIP and the aggregate guard fails, then restoring RUN and observing success. Prove the workflow trigger guard rejects addition of pull_request or merge_group."
  ],
  "out_of_scope": [
    "Endpoint declarations, config resolution, credentials, reachability probes, and runtime dispatch are owned by S01 and S02.",
    "Probe semantics, observation generation, dialect-table generation/checking, and runtime request shaping are owned by S03.",
    "Making external providers reliable, provisioning paid credentials, or starting desktop/GPU local services on GitHub-hosted runners is outside repository control.",
    "Using live provider calls as a pull-request merge gate is forbidden.",
    "Automatically editing, committing, pushing, or opening a pull request for dialect drift is forbidden.",
    "Promoting endpoint-level observations into per-model role capability claims remains S04-provider-registry and S05-capability-eligibility in the contract-edge-gates release."
  ],
  "covers_needs": [
    "N-04"
  ],
  "effort_complexity": {
    "effort": "high",
    "complexity": "high",
    "quadrant": "beast",
    "rationale": "The implementation coordinates a multi-provider workflow, mandatory and optional availability semantics, aggregate schema enforcement, artifact flow, drift detection, and non-deterministic external systems. Although the repository touchpoint count is modest, secret mapping and live-run failure modes are emergent and operationally complex.",
    "confirmed_by_implementer": false
  },
  "risks": [
    {
      "id": "R-01",
      "description": "A green workflow silently omits a provider or skips both mandatory canaries.",
      "mitigation": "Compare workflow rows with the runtime endpoint table, require exactly one aggregate row per descriptor, prohibit SKIP for mandatory rows, and fail when the executed mandatory count is zero."
    },
    {
      "id": "R-02",
      "description": "GitHub-hosted localhost is mistaken for an operator's local service, producing misleading failures or coverage claims.",
      "mitigation": "Local rows require explicit runner/base/model configuration and otherwise emit a closed, explicit SKIP reason; release proof distinguishes a real RUN from a skip."
    },
    {
      "id": "R-03",
      "description": "Provider credentials or model payloads leak through logs, row metadata, uploaded observations, or failure diagnostics.",
      "mitigation": "Pass credentials only through masked environment secrets, reuse S03's secret-safe records, aggregate allowlisted metadata, inject canary values in deterministic tests, and run the public-safe scan."
    },
    {
      "id": "R-04",
      "description": "Nightly drift mutates the repository or is normalized away, hiding a provider wire change.",
      "mitigation": "Generate to an artifact path, run read-only check, assert the worktree is unchanged, fail on byte drift, and upload both source observations and candidate table for human review."
    },
    {
      "id": "R-05",
      "description": "External outages make the PR gate flaky and encourage maintainers to disable the guard.",
      "mitigation": "Assert schedule/workflow_dispatch are the only triggers and keep every live job outside ci.yml; live failures remain visible without blocking unrelated pull requests."
    }
  ],
  "acceptance_criteria": [
    {
      "id": "AC-01",
      "text": "WHEN `.github/workflows/live.yml` is mechanically compared with the declared OpenAI-compatible endpoint descriptors THE SYSTEM SHALL contain exactly one row for every descriptor and no unknown or duplicate provider row; adding or removing an endpoint on only one side SHALL fail the test (C-01, C-07, N-04).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/model/live_matrix_test.go:TestLiveWorkflowCoversEveryDeclaredOAIEndpoint"
      ]
    },
    {
      "id": "AC-02",
      "text": "WHILE the live workflow is triggered by its nightly schedule THE SYSTEM SHALL require `ollama-cloud` and the existing `openai` hosted canary to execute; missing secret/model/config, unreachable transport, failed conformance command, or missing result SHALL produce FAIL and a non-zero workflow, and neither mandatory row SHALL emit SKIP (C-02, C-04, C-05, C-07, N-04).",
      "ears_pattern": "state-driven",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestMandatoryRowsFailClosed"
      ]
    },
    {
      "id": "AC-03",
      "text": "WHEN an optional local or hosted row lacks its explicitly required runner, base URL, model, or credential THE SYSTEM SHALL emit exactly one schema-valid SKIP row with a closed reason code; WHEN it is configured and its probe has transport, auth, protocol, or record failure THE SYSTEM SHALL emit FAIL rather than SKIP (C-03, C-04, C-07, N-04).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestOptionalSkipAndFailureClassification"
      ]
    },
    {
      "id": "AC-04",
      "text": "WHEN all endpoint jobs finish THE SYSTEM SHALL produce one endpoint-conformance-matrix-v1 aggregate in stable provider order with exactly the declared rows, status RUN|FAIL|SKIP, closed reason codes, safe probe-model identifiers, and relative record references; duplicate, missing, unknown, malformed, secret-bearing, or zero-executed-mandatory input SHALL fail aggregation (C-07, N-04).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestAggregateMatrixContract"
      ]
    },
    {
      "id": "AC-05",
      "text": "WHEN a row is classified RUN THE SYSTEM SHALL invoke the built `sworn endpoint-conformance probe` command for its declared provider/model and upload the schema-valid raw observation; rejected optional features SHALL remain classified observations, while command or schema failure SHALL make the row FAIL (C-06, C-07, N-04).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestRowRunnerUsesSwornCommandExitAndRecord"
      ]
    },
    {
      "id": "AC-06",
      "text": "WHEN successful observations regenerate a byte-different dialect table THE SYSTEM SHALL make `sworn endpoint-conformance check` fail the workflow, upload the raw observations and candidate normalized table, and leave tracked repository files unchanged; equality SHALL succeed without an upload requirement and no workflow step SHALL commit, push, comment, or open a pull request (C-06, N-04).",
      "ears_pattern": "event-driven",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestDialectDriftFailsWithoutRepositoryMutation"
      ]
    },
    {
      "id": "AC-07",
      "text": "IF a mandatory row is mutation-tested as SKIP THEN matrix enforcement SHALL fail, and WHEN the row is restored as RUN enforcement SHALL pass; proof SHALL retain both outcomes (C-07, N-04).",
      "ears_pattern": "unwanted-behaviour",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestMandatorySkipMutationProof"
      ]
    },
    {
      "id": "AC-08",
      "text": "THE SYSTEM SHALL expose the live matrix only through `schedule` and `workflow_dispatch`; adding `pull_request` or `merge_group` SHALL fail the workflow-shape guard, and `.github/workflows/ci.yml` SHALL not invoke live endpoint scripts or set SWORN_LIVE_TESTS (N-04).",
      "ears_pattern": "ubiquitous",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestLiveWorkflowIsOutsidePRGate"
      ]
    },
    {
      "id": "AC-09",
      "text": "IF canary credential, Authorization, endpoint-userinfo/query, request-body, response-body, prompt, or model-output values are injected THEN live row logs, matrix JSON, dialect observations, artifacts, and failure diagnostics SHALL contain none of them, and public-safe-scan plus go test ./internal/liveconformance/... ./internal/model/... SHALL pass (C-02, C-05, C-06, C-07, N-04).",
      "ears_pattern": "unwanted-behaviour",
      "test_refs": [
        "internal/liveconformance/matrix_test.go:TestLiveArtifactsAreSecretAndPayloadSafe"
      ]
    },
    {
      "id": "AC-10",
      "text": "WHEN release reachability is captured THE SYSTEM SHALL preserve the live workflow URL/run identifier and aggregate artifact showing mandatory Ollama Cloud and OpenAI RUN results, plus either a real local Ollama RUN observation or one explicit local Ollama SKIP reason; a missing local row is not acceptable evidence (C-07, N-04).",
      "ears_pattern": "event-driven",
      "test_refs": [
        ".github/workflows/live.yml:workflow_dispatch endpoint matrix run"
      ]
    }
  ]
}
