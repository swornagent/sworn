---
title: 'Release board — 2026-07-01-loop-cli-ux'
description: 'Rendered view of board.json + slice records. Generated by `sworn render 2026-07-01-loop-cli-ux` — do not hand-edit; edit board.json and re-render.'
---

# Release board: 2026-07-01-loop-cli-ux

> Generated by `sworn render`. This file is a deterministic view of `board.json`
> plus each slice's `spec.json`/`status.json` — never hand-authored.

## Tracks

| Track | Slices | depends_on | State |
|-------|--------|------------|-------|
| `T1-loop-cli-ux` | `S01-active-release-store`, `S02-loop-explicit-standalone`, `S03-loop-default-from-store` | — | planned |

## Slices

| Slice | Track | Outcome | State | E×C |
|-------|-------|---------|-------|-----|
| `S01-active-release-store` | `T1-loop-cli-ux` | Running `sworn use <release>` validates the release against the board oracle and persists it as the active release in `.sworn/sworn.db`; running `sworn use` with no arguments prints the current selection, or a clear "no active release" message if none is set. | planned | quick |
| `S02-loop-explicit-standalone` | `T1-loop-cli-ux` | Running `sworn loop --release <name>` drives that release's autonomous loop without also passing `--parallel` — board-driven mode is the default whenever `--release` is given, and the dead `--base` flag is removed from the flag surface entirely. | planned | quick |
| `S03-loop-default-from-store` | `T1-loop-cli-ux` | Running a bare `sworn loop` (no flags at all) resolves the active release from S01's store and drives it exactly as `sworn loop --release <name>` (S02) does — restoring the coach-loop `coach use <release>` + `coach loop` two-command ergonomics on sworn's own CLI. | planned | quick |

## Touchpoint matrix

Every planned-write file × track. A file marked under two tracks is a
collision — disjointness is what licenses tracks to run independently.

| File | T1-loop-cli-ux |
|------|----|
| `cmd/sworn/main.go` | ✓ |
| `cmd/sworn/run.go` | ✓ |
| `cmd/sworn/run_test.go` | ✓ |
| `cmd/sworn/use.go` | ✓ |
| `cmd/sworn/use_test.go` | ✓ |
| `internal/db/db.go` | ✓ |
| `internal/db/db_test.go` | ✓ |

## Dependency graph

```
T1-loop-cli-ux  (root)
```
