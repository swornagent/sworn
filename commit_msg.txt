feat(verify): land S10-no-mock-boundary — fail-closed on undeclared boundary mocks

Implement the no-mock-boundary enforcement:
- Boundary-mock detection (CheckBoundaryMocks) in internal/verify/verify.go
  scans diff for mock/stub/fake at validated DB/auth/entitlement boundaries
- Undeclared boundary mocks cause verify.Run() to return FAIL/boundary_mock
  before model dispatch (first-pass gate, no model cost incurred)
- Declared mocks (matching open_deferrals with why+tracking+acknowledgement)
  are allowed and surfaced as known deferrals
- Input struct gains OpenDeferrals field for cross-referencing
- Stop-don't-mock principle added as hard constraint in implementer.md
- No-mock-boundary enforcement documented in rule 10 rule file
- 12 tests covering: undeclared db/auth/entitlement fails, declared passes,
  non-boundary mock not flagged, fake/stub variants, Run integration tests

Design decisions:
- Heuristic detection: line must match BOTH mock marker AND boundary pattern
  to be flagged (conservative, biases toward surfacing ambiguous cases)
- Declared-mock matching: case-insensitive substring on boundary name +
  mock/fake/stub keyword in deferral text
- Blocked-on-environment state: implementer records in journal.md, not a
  new state.go state — the spec requires support for recording, not a new
  state machine transition

No deferrals carried — S10 bans undeclared deferrals and has none.